services:
  - type: web
    name: cricket-live-server
    env: node
    plan: free
    rootDir: server
    buildCommand: npm install
    startCommand: node index.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: CLIENT_URL
        sync: false   # You set this manually in Render dashboard after Netlify deploy
